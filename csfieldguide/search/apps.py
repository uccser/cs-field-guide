"""Application configuration for the search application."""

from django.apps import AppConfig


class SearchConfig(AppConfig):
    """Configuration object for the search application."""

    name = "search"
