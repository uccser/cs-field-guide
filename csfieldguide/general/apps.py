"""Application configuration for the general application."""

from django.apps import AppConfig


class GeneralConfig(AppConfig):
    """Configuration object for the general application."""

    name = "general"
